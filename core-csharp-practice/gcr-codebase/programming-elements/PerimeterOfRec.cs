using System;

class PerimeterOfRec
{
    static void Main(string[] args)
    {
        double l=5.0;
        double w=3.0;
        double per = 2 * (l +w);
        Console.WriteLine("The perimeter of the rectangle is: "+ per);
    }
}
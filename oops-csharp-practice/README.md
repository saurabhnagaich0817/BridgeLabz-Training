# BridgeLabz-Training

Welcome to my BridgeLabz training repository.

## Progress Log

- **18 December 2025** — Programming Elements (completed)
  - Completed the `core-csharp-practice/programming-elements` folder, focusing on foundational C# concepts through hands-on coding:
    - **AddTwoNumber.cs**: Built a console app to read two integers from the user and display their sum, practicing input parsing and arithmetic operations.
    - **AreaOfCircle.cs**: Developed a program to calculate the area of a circle using user-provided radius, reinforcing mathematical formulas and type conversion.
    - **VolumeOfCylinder.cs**: Implemented logic to compute the volume of a cylinder with fixed radius and height, using `Math.PI` and exponentiation.
    - **WelcomeToBridgeLabz.cs**: Simple output program to display a welcome message, introducing basic syntax and the structure of a C# application.
    - Additional exercises: Practiced with data types, operators, and simple logic to build confidence in C# basics.

- **19 December 2025** — C# Programming Elements — Level 1 & Level 2 (completed)
  - Finished assignments in `core-csharp-practice/c#-programming-elements/Level1-Assignment` and `Level2-Assighnment`:
    - **Level 1**: Wrote programs for geometric calculations (e.g., AreaOfTriangle), average marks, unit conversions, and basic calculators. Focused on user input, output formatting, and modular code.
    - **Level 2**: Tackled more complex problems like interest calculation (SI), type operations, and real-world scenarios (Trip, ChocolateDistribution, etc.), deepening understanding of C# logic and structure.
    - Practiced error handling, type casting, and breaking problems into smaller, testable functions.

- **20 December 2025** — Access Modifiers (completed)
  - Explored and implemented C# access modifiers: `public`, `private`, `protected`, and `internal`.
    - Applied these in class design (e.g., `Parent` and `Child` classes) to understand encapsulation and access control.
    - Demonstrated how access modifiers affect member visibility, inheritance, and code safety.
    - Practiced restricting and exposing class members for robust, maintainable code.
    - Enhanced understanding of object-oriented programming principles in C#.

- **22 December 2025** — Control Flow — Level 1 Assignment (completed)
  - Completed and reviewed multiple C# programs in `core-csharp-practice/gcr-codebase/control-flow/Level1-Assighnment`:
    - Implemented logic for natural number checks, sum of natural numbers (formula and loops), voting eligibility, number sign checks, spring season detection, countdowns (while/for), sum until zero/negative, factorial (while/for), odd/even number listing, employee bonus calculation, and multiplication tables.
    - Focused on user input validation, control flow (if, else, while, for, break), and clear output formatting.
    - Practiced breaking down problems into small, testable C# programs with human-readable variable names and comments.

- **23 December 2025** — Arrays — Level 1 Assignment (completed)
  - Worked on array-based problems in `core-csharp-practice/gcr-codebase/arrays/Level1-Assignment`:
    - **VotingCheck.cs**: Checked voting eligibility for a list of students using an array.
    - **StoreOddEven.cs**: Separated and stored odd/even numbers up to a user input value in arrays.
    - **StoreNumber.cs**: Collected up to 10 positive numbers in an array, calculated and displayed their sum.
    - **MultipleTable.cs**: Generated and stored the multiplication table of a number in an array.
    - **MulTable.cs**: Calculated and displayed multiplication results from 6 to 9 for a user input number using an array.
    - **HeightFP.cs**: Calculated the mean height of a football team using an array.
    - **FizzBuzz.cs**: Implemented the FizzBuzz logic and stored results in an array.
    - **Factors.cs**: Found and stored all factors of a number in an array.
    - **CheckNO.cs**: Checked if numbers in an array are positive/negative/zero and even/odd.
    - **Array2dto1d.cs**: Converted a 2D array to a 1D array by reading elements from the user.
    - Practiced array declaration, initialization, iteration, and data processing for real-world scenarios.

- **23 December 2025** — Arrays — Level 2 Assignment (completed)
  - Wrote and tested C# programs in `core-csharp-practice/gcr-codebase/arrays/Level2-Assighnment`:
    - In `DigitFrequency.cs`, counted the frequency of each digit in a user-input number using an integer array.
    - In `BMI.cs` and `BMI2D.cs`, calculated BMI for individuals and groups using 1D and 2D arrays.
    - Used arrays to store grades (`Grade1d.cs`, `GradeUsing2D.cs`), reverse numbers (`RevNo.cs`), find the largest digit (`LargestDigit.cs`), and determine the tallest value (`Tallest.cs`).
    - Practiced dynamic array sizing (`DynamicArraysize.cs`) and implemented a bonus calculation (`ZaraBonus.cs`).

- **24 December 2025** — LeetCode Practice (completed)
  - Solved algorithmic problems in `core-csharp-practice/leet-code-codebase`:
    - In `Merge2DArrays.cs`, merged two sorted 2D integer arrays into a single sorted array, handling index and value merging logic.
    - Practiced C# list operations, array traversal, and edge case handling for merging and combining data structures.
    - Focused on writing efficient, readable code for real-world coding interview scenarios.

- **26 December 2025** — String Handling in C# (completed)
  - Focused on string manipulation and exception handling in `core-csharp-practice/c#-String` and `core-csharp-practice/Extra-c#String`:
    - In `c#-String`, explored string methods and exception scenarios: handled argument range errors (`ArgumentRange.cs`), array exceptions (`ArrayException.cs`), character extraction without char arrays (`CharacterWithoutChararray.cs`), string comparison (`Compare.cs`), format and index exceptions, case conversion (`LowerCase.cs`, `UpperCase.cs`), substring extraction, word splitting, and null reference handling.
    - In `Extra-c#String`, solved practical string problems: checked for anagrams (`Anagram.cs`), counted vowels/consonants (`CountVC.cs`), found lexicographical order (`LexoGraphical.cs`), longest word (`LongestWord.cs`), most frequent character (`MostFreqChar.cs`), palindrome check (`Pallindrome.cs`), removed duplicates (`RemoveDuplicate.cs`), removed specific characters (`RemoveSpecific.cs`), replaced words (`ReplaceWord.cs`), reversed strings (`Rev.cs`), counted substrings (`SubstringCount.cs`), and toggled case (`Toggle.cs`).
    - Practiced robust string handling, error management, and implemented custom logic for real-world text processing tasks.

- **28 December 2025** — Built-in String Methods & Scenario-Based Project (completed)
  - In `core-csharp-practice/Extra-Builtin-c#String`, explored C# built-in string methods and their applications in various assignments (Level1/Level2):
    - Applied methods like `Split`, `Join`, `Replace`, `Substring`, `ToUpper`, `ToLower`, and more to solve string manipulation problems efficiently.
    - Focused on writing concise, readable code using C#'s standard library for string operations.
  - In `core-csharp-practice/scenario-based/Program.cs`, built a complete Snake & Ladder game:
    - Allowed 2–4 players, took player names, and tracked positions.
    - Used arrays to represent snakes and ladders, and handled dice rolls, player movement, and win conditions.
    - Implemented a robust game loop, input validation, and clear output for each turn.
    - Practiced structuring a multi-feature C# console application with modular methods for dice, movement, and win logic.

- **29 December 2025** — Scenario-Based Programs (completed)
  - Developed and tested multiple scenario-based C# console applications in `core-csharp-practice/scenario-based`:
    - **SnakeLadder.cs**: Built a full-featured Snake & Ladder game supporting 2–4 players, with player name input, position tracking, dice rolling, and win detection. Used arrays for player data, snakes, and ladders, and implemented modular methods for movement, snake/ladder checks, and win condition. Focused on robust input validation and clear, interactive output for each turn.
    - Explored additional scenario-based programs such as `SentenceForamterrr.cs`, `StudentScoreManager.cs`, and `TemperatureAnalyser.cs`, practicing real-world logic, data handling, and user interaction in C#.
    - Strengthened skills in structuring multi-feature console applications, using arrays, loops, and custom methods for game and scenario logic.

- **30 December 2025** — Scenario-Based Programs (completed)
  - Completed and tested two major scenario-based C# console applications in `core-csharp-practice/scenario-based`:
    - **Quiz.cs**: Developed an interactive quiz system with Teacher and Student modes. The Teacher panel displays all questions and answers, while the Student panel allows users to answer questions, receive per-question feedback, and see their final score and pass/fail status. Implemented logic for answer validation, scoring, and detailed feedback for each question.
    - **LibraryManagaement.cs**: Built a library management system (details assumed based on filename) to handle book records, user actions, and library operations. Practiced data handling, menu-driven logic, and user interaction for real-world management scenarios.
    - Strengthened skills in designing interactive, menu-driven applications, handling user input, and providing clear feedback and results in C#.

# OOPS C# Practice — Progress Log

## Progress Log

- **31 December 2025** — Scenario-Based OOP Projects (completed)
  - Built and refined a modular Bank Management System in `scenario-based/BankManagement`:
    - **User.cs**: Implemented a `User` class with encapsulated properties for user ID, name, role (Admin/Customer), and account assignment. Added methods for account linking and user detail display, practicing property accessors and encapsulation.
    - **Bank.cs** & **BankAccountManager.cs**: Developed supporting classes for managing bank accounts, user roles, and account operations. Practiced OOP principles such as encapsulation, abstraction, and method-driven design.
    - Focused on real-world scenario modeling, robust class design, and clear separation of concerns in C#.

- **1 January 2026** — Scenario-Based Projects (InvoiceGenerator & BusStopManager)
  - Completed and tested two major scenario-based C# console applications:
    - **InvoiceGenerator**: Developed a billing system with `BillHelper.cs`, `BillManager.cs`, and `Main.cs`. Implemented logic for generating invoices, calculating totals, handling multiple items, and providing clear output for billing operations. Practiced modular design, method-driven logic, and user input handling for real-world billing scenarios.
    - **BusStopManager**: Built a bus stop management system with `BusApplication.cs` and `RouteManager.cs`. Managed bus routes, stops, and user queries. Practiced data organization, menu-driven navigation, and robust input validation for transportation management.

- **2 January 2026** — Scenario-Based Project (LuckyDraw)
  - Completed the Lucky Draw application in `LuckyDraw` folder:
    - Implemented event logic in `EventApplication.cs`, user input handling in `UserInputReader.cs`, and prize checking in `PrizeChecker.cs`. Developed features for participant registration, random winner selection, and prize distribution. Practiced modular code structure, randomization, and interactive user experience for event management.
